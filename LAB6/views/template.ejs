<!DOCTYPE html>
        <html>
            <head>
                <meta charset='utf-8'>
                <meta name="viewport" content="width=device-width, initial-scale=1">
                <title>HOME</title>
                <link rel='stylesheet' type='text/css' href='/main.css'>
            </head>
            <body>
                <div class="header"> 
                    <h1>Uncle Ben's Farm</h1>
                </div>
                <ul class="navbar">
                    <li><a href="/" class="active">Home</a></li>
                    <li><a href="/login">Log in</a></li>
                    <li><a href="/signup">Signup</a></li>
                </ul>
                <div class="page-content">
                    <div class="product-display"> 
                        <img src="/<%=product.pro_img%>" alt="Product Key Photo"/>
                    </div>
                    <div class="product-buy">
                        <p class="product-name"> <span>#<%=product.pro_id%></span> <%=product.pro_title%></p>
                        <span class="info" id="category"><%=product.pro_cate%></span><br>
                        <span class="info" id="price">$<%=product.pro_price%></span><br><br><br>
                        <span class="info" id="review"> Review: </span>
                        <form action="/addComment/<%=product.pro_id%>" method="POST" class="add-comment">
                            <div>
                                <input type="text" class="line-comment" id="textinput" name="newcomment" placeholder="Write a review...">
                            </div> <br>
                            <div class="center">
                                <input type="submit" value="Upload" class="button" id="submit">
                                <input type="reset" value="Clear" class="button" id="reset">
                            </div>
                        </form> <br>
                        <div class="review"> 
                            <% review.forEach((line) => { %>
                                <% if (line.product_id == product.pro_id) { %>
                                    <div class="single_comment">
                                        <%=line.comment%>
                                    </div>
                                <% } %>
                            <% }); %>
                        </div>
                    </div>   
                </div>
            </body>
        </html>